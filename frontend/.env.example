VITE_API_URL=http://localhost:5000/api

# Optional: shows the seeded demo login credentials on the login page.
# Intended only for the public demo deployment — leave unset/false elsewhere.
VITE_SHOW_DEMO_CREDENTIALS=false
